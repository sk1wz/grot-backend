### Принцип работы

## Для single отправки

# Front -> Backend API -> обращение к endpoint -> списание баланса, добавление в бд со статусом (Pending) - это все одна транзакция -> Добавление в очередь (у каждой сущности свой воркер и своя очередь) -> Общение к API сервиса пробива -> получение ответа, обновление в бд (обновление статуса и serviceId) -> успешное завершение (Обновление в бд).
